<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BjjTrainer.Views.Lessons.SubLessonPage"
             Title="{Binding SectionTitle}">

    <ContentPage.Resources>
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="TextColor" Value="Black"/>
        </Style>
        <Style TargetType="StackLayout">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BackgroundColor" Value="White"/>
            <Setter Property="Margin" Value="10"/>
        </Style>
        <x:Double x:Key="boxSize">50</x:Double>
        <x:Int32 x:Key="rowHeight">225</x:Int32>
    </ContentPage.Resources>

    <ContentPage.Content>
        <ListView x:Name="SubLessonsListView"
          ItemsSource="{Binding SubLessons}"
          RowHeight="{StaticResource rowHeight}"
          VerticalOptions="FillAndExpand">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Padding="10"
                             Orientation="Vertical"
                             Spacing="5"
                             BackgroundColor="#F9F9F9">
                            <Label Text="{Binding Title}" FontAttributes="Bold" FontSize="20" TextColor="#333"/>
                            <Label Text="{Binding Content}" FontSize="14" TextColor="#555" LineBreakMode="TailTruncation" MaxLines="3"/>
                            <StackLayout Orientation="Horizontal" Spacing="15">
                                <Label Text="{Binding SkillLevel}" FontSize="14" TextColor="#007AFF"/>
                                <Label Text="Duration: " FontSize="14" TextColor="#333"/>
                                <Label Text="{Binding Duration}" FontSize="14" TextColor="#007AFF"/>
                            </StackLayout>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>
