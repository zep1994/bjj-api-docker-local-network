<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="BjjTrainer.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:BjjTrainer"
    xmlns:controls="clr-namespace:BjjTrainer.Controls"
    xmlns:views="clr-namespace:BjjTrainer.Views"
    xmlns:users="clr-namespace:BjjTrainer.Views.Users"
    xmlns:viewModels="clr-namespace:BjjTrainer.ViewModels"
    x:DataType="viewModels:MainPageViewModel"
    Shell.FlyoutBehavior="Flyout"
    Title="BjjTrainer">

    <Shell.Resources>
        <Style x:Key="BaseStyle"
               TargetType="Element">
            <Setter Property="Shell.BackgroundColor"
                    Value="#000" />
            <Setter Property="Shell.ForegroundColor"
                    Value="Red" />
            <Setter Property="Shell.TitleColor"
                    Value="White" />
            <Setter Property="Shell.DisabledColor"
                    Value="Green" />
            <Setter Property="Shell.UnselectedColor"
                    Value="Purple" />
        </Style>
        <Style TargetType="ShellItem"
               BasedOn="{StaticResource BaseStyle}" />
        <Style x:Key="DomesticShell"
               TargetType="Element"
               BasedOn="{StaticResource BaseStyle}">
            <Setter Property="Shell.BackgroundColor"
                    Value="Black" />
        </Style>
        <Style x:Key="MonkeysShell"
               TargetType="Element"
               BasedOn="{StaticResource BaseStyle}">
            <Setter Property="Shell.BackgroundColor"
                    Value="#689F39" />
        </Style>
        <Style x:Key="ElephantsShell"
               TargetType="Element"
               BasedOn="{StaticResource BaseStyle}">
            <Setter Property="Shell.BackgroundColor"
                    Value="#ED3B3B" />
        </Style>
        <Style x:Key="BearsShell"
               TargetType="Element"
               BasedOn="{StaticResource BaseStyle}">
            <Setter Property="Shell.BackgroundColor"
                    Value="#546DFE" />
        </Style>
        <Style x:Key="AboutShell"
               TargetType="Element"
               BasedOn="{StaticResource BaseStyle}">
            <Setter Property="Shell.BackgroundColor"
                    Value="#96d1ff" />
        </Style>
    </Shell.Resources>

    <Shell.FlyoutHeader>
        <controls:FlyoutHeader />
    </Shell.FlyoutHeader>

    <!-- Home Page -->
    <FlyoutItem Title="Home">
        <ShellContent Route="Home" Style="{StaticResource DomesticShell}" Title="Home" ContentTemplate="{DataTemplate views:MainPage}" />
        <ShellContent Title="Login" ContentTemplate="{DataTemplate users:LoginPage}" />
        <ShellContent Title="Signup" ContentTemplate="{DataTemplate users:SignupPage}" />
        <ShellContent Title="Logout" ContentTemplate="{DataTemplate users:LogoutPage}" />
    </FlyoutItem>
</Shell>
